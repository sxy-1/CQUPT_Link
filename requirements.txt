# 跨平台核心依赖
PyQt6>=6.4.2
PyQt6-Fluent-Widgets>=1.4.3
PyQt6-Frameless-Window>=0.3.6
requests>=2.31.0
psutil>=5.9.7


# 网络和解析
urllib3>=2.1.0
certifi>=2023.11.17
charset-normalizer>=3.3.2
idna>=3.6

# 开发和构建工具
pyinstaller>=6.3.0

# Windows特定依赖 (仅在Windows上需要)
chardet>=5.2.0; sys_platform == "win32"

# macOS特定依赖 (仅在macOS上需要)
pyobjc-framework-Cocoa>=11.1; sys_platform == "darwin"
pyobjc-framework-SystemConfiguration>=11.1; sys_platform == "darwin"
pyobjc-framework-CoreWLAN>=11.1; sys_platform == "darwin"

# 可选依赖 (用于其他功能)
DrissionPage>=4.0.4.21

